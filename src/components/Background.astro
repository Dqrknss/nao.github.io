---
import { Image } from "astro:assets";

import BlobLeft from "../assets/blob-left.svg";
import BlobRight from "../assets/blob-right.svg";
import BackgroundPhoto from "../assets/background.png";
---
<div class="background">
	<Image
		class="background-image"
		src={BackgroundPhoto}
		alt=""
		loading="eager"
	/>
	<div class="blobs">
		<Image
			class="blob blob--left"
			src={BlobLeft}
			alt="Left blob"
			loading="eager"
		/>
		<Image
			class="blob blob--right"
			src={BlobRight}
			alt="Right blob"
			loading="eager"
		/>
	</div>
</div>

<style>
	.background-image {
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		object-fit: cover;
		z-index: -2;
	}

	.blobs {
		position: fixed;
		top: 0;
		bottom: 0;
		left: 0;
		right: 0;
		z-index: -1;
		overflow: hidden;
		opacity: 0.5;
		--blob-size: 1000px;
	}

	.blob {
		position: absolute;
		width: var(--blob-size);
		height: var(--blob-size);
		transform-origin: center;
	}

	.blob--left {
		left: calc(var(--blob-size) * -0.17);
		bottom: calc(var(--blob-size) * -0.75);
		transform: rotate(-60deg) scale(1, 2)
	}
	
	.blob--right {
		right: calc(var(--blob-size) * -0);
		top: calc(var(--blob-size) * -0.75);
		transform: rotate(120deg) scale(1, 2)
	}
</style>